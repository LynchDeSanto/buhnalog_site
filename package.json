{
  "name": "balance-profi-landing",
  "version": "1.0.0",
  "private": true,
  "description": "Landing page with Node.js backend for Telegram contact form submissions.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "check": "node --check server.js && node --check script.js"
  },
  "engines": {
    "node": ">=18"
  }
}
